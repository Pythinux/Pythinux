NAME
    cat - view the contents of files
DESCRIPTION
    CAT is a tool used to view the contents of a file, or multiple files sequentially.
SYNTAX
    cat [list of files]
        Prints the contents of one or more files (one file per argument).
LIBRARY
    cat.cat(filename, user=None)
        Opens a file and prints its contents. If user is not specified, the current user is used.
