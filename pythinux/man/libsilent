NAME
    libsilent - Library for silencing the terminal
DESCRIPTION
    Sometimes you need to make the terminal not output anything. When this happens, you need libsilent.
LIBRARY
    libsilent.silence()
        Silences the terminal and returns stdout. MAKE SURE TO SAVE THIS TO A VARIABLE!
    libsilent.unsilence(stdout)
        Takes the stdout from libsilent.silence() and un-silences the terminal.
EXAMPLE
    libsilent = load_program("libsilent", currentUser)
    print("This message is visible!")
    stdout = libsilent.silence()
    print("This message is silent!")
    libsilent.unsilence(stdout)
    print("This message is visible!")
SEE ALSO
    lib: list of libraries in Pythinux
